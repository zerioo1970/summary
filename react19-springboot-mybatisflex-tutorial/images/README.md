# 图片资源目录

本目录存放教程各章节引用的截图。

教程正文中形如 `![...](images/xxx.png)` 的引用为**截图占位**，标注了该处应看到的界面效果。你在按教程实际操作时，可将对应的软件安装/操作截图按同名文件放入本目录，Markdown 即可正常显示。

> 教程中的 `mermaid` 流程图/架构图无需图片，在 GitHub、VS Code、Typora 等支持 mermaid 的环境中会直接渲染。
