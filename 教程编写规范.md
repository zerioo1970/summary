# 教程编写规范

> **本文件是教程编写规则的唯一权威来源。**
> 为 zerioo1970 编写任何技术教程 / 学习材料时，编写者（AI 或本人）必须遵守。
> 修改本文件即修改规范；改完请一并提醒 AI 更新全局记忆，使其在其他仓库的会话中同步生效。
>
> 参考实现：[`LangChain1.3教程/`](LangChain1.3教程/README.md) —— 结构与文风以它为基准。

---

## 1. 读者画像（不可假设读者是编程新人）

| 项 | 内容 |
|---|---|
| 背景 | 20 年 C# ASP.NET WebForm + SQL Server 企业开发经验 |
| 强项 | SQL、关系型数据建模、业务抽象、调试直觉 |
| 弱项 | 大学数学已遗忘；现代前端工程链路（打包、模块、类型）不熟 |
| 定位 | **应用开发**，不做算法研究、不做框架源码研究 |
| 时间 | 每天最多 2 小时，经常被打断 |
| 语言 | 全部中文 |

推论：

- **不要解释"什么是变量""什么是循环""什么是数据库表"。**
- 要解释的是：这门技术**为什么这样设计**、它和熟悉的做法**差在哪**、**什么时候不该用**。
- 目标是"能跑起来的东西"，不是概念大全。

## 2. 单元切分（硬指标）

- 一章 = 一次学习单元：**通读 + 动手 ≤ 90 分钟**。
- 单章正文 ≤ **400 行** Markdown。超了拆成两章，不交付"超长全集"。
- **单章引入的新概念 ≤ 3 个。** 第 4 个一律推迟到下一章。
- 每章必须**能独立跑通**，不允许出现"环境搭好了但要等下一章才能运行"的悬空状态。

## 3. 每章固定骨架（顺序不得调整）

```
# 第 N 章 <标题>

## 这一章解决什么问题
（1–3 句。说痛点场景，不罗列知识点。
  反例："本章介绍 useState、useEffect"
  正例："页面上的数字点一下就要变，但直接改变量页面没反应——本章解决这个。"）

## 开始之前
- 上一章你已经会了：……
- 需要的环境 / 前置命令：……

## 正文
（若干小节，每个概念内部严格按第 4 节四步走）

## 🧱 类比
（按第 7 节分场景规则；前端主题跳过本节，不要硬凑）

## ⚠️ 踩坑警告
（表格：现象 → 原因 → 怎么改。≥ 2 条，必须真实高频，不许编）

## 🎯 动手练习
（1–2 个任务，每个带**验收标准**=看到什么现象算做对。
  末尾提示："卡住了看文末参考答案"）

## 📌 一句话总结
（一句 ≤ 40 字。禁止"本章我们学习了 A、B、C"式复述）

## 下一章
（下一章要解决的问题，一句话）

<分隔 + 30 空行，见 3.1>

## 参考答案
```

### 3.1 参考答案的隔离方式

参考答案**必须是文件最后一节**，与正文保持足够距离，避免滚动时误看到：

```markdown
---

<!-- 以下是参考答案，先自己动手，卡住了再往下翻 -->

（此处 30+ 个空行）

## 参考答案
```

- 给**完整可运行代码** + 一句思路说明（为什么这么写）。
- 正文中不得以任何形式提前透露答案代码。

## 4. 每个概念的四步走：先能跑，再讲为什么

1. **🖼 图解**（可选但推荐；须符合第 9 节图表规则）
2. **💻 最小可运行代码**（完整，复制就能跑）
3. **🔍 拆解运行结果**（跑起来看到什么现象 / 什么输出，再逐行解释关键行）
4. **改坏一处试试**（故意改错一处，说明会怎么报错、为什么）

**禁止**先铺垫一大段理论再上代码。

每个概念必须三件套齐全：

- 一句白话定义
- 一个**放在读者业务语境**的具体场景（报表、审批流、价格申请、股票数据……）
- **一个反例：什么时候不该用它**（必填项）

## 5. 代码规范

- 代码必须**完整可运行**。禁止 `...`、`# 其余略`、`// 同上` 代替代码体。
- 代码块第一行注释写出**文件路径**，例如 `// src/components/PriceTable.tsx`。
- 长代码同时落盘到 `code/chNN/`，正文与仓库文件保持一致。
- 关键行加**中文注释**；注释解释"**为什么**"，不复述"这行在做什么"。
- 命令行片段标明**在哪个目录执行**。
- 交付前代码**实机验证**；确实无法执行时显式标注 `⚠️ 本段未实机验证`。
- 禁止未验证的版本号、API 名称、命令参数。不确定就查，查不到就标注。

## 6. 技术栈基线

每套教程的 README 顶部必须声明**版本基线**（照 LangChain 教程的写法）。当前各栈基线：

| 领域 | 基线 |
|---|---|
| 前端 | **TypeScript**（禁纯 JS）、React 19、Vite（`react-ts` 模板）；`strict` 开启，**禁止 `any`**；props 类型、事件类型、Hook 泛型、判别联合写全 |
| 后端 | **Spring Boot 3.5 + MyBatis-Flex + SQL Server + Gradle**（不是 Maven，不是 ASP.NET） |
| Python / AI | Python 3.10+；LangChain 系按 README 声明的 `1.3.x` 基线 |
| 历史栈 | C# ASP.NET WebForm + SQL Server 仅作为**已有经验**参与类比，不作为新教程的示例栈 |

## 7. 类比规则（分场景，不可混用）

| 主题 | 策略 |
|---|---|
| React / 前端 / TypeScript | **禁止** C# / ASP.NET WebForm 类比。前端是全新学习，按其自身概念体系讲，类比只会增加噪音 |
| Python / Pandas / 机器学习 / LangChain | **可以** C# / SQL 类比（DataFrame ↔ 结果集，groupby ↔ GROUP BY） |
| 其他领域 | 优先日常生活类比；没有好类比就直说，不要硬凑 |

## 8. 数学规则

- 只允许：均值、标准差、百分比变化、差分、相关性、概率直觉。
- **禁止公式推导，禁止求导、积分、矩阵符号。**
- 必须用**具体数值示例**代替符号表达。
  - 反例：`σ = √(Σ(xᵢ-μ)²/n)`
  - 正例："这 5 天涨幅 1%、2%、-1%、0%、3%，平均 1%，波动大约 1.5 个百分点。"

## 9. 图表规则（硬性要求：打开就能看全，不用缩放）

- Mermaid 图**最宽不超过 2 列节点**。
- 多层流程用 `graph TB` / `graph TD`，**不用 `graph LR`**。
- **禁止**用带 `direction` 的 `subgraph` 做左右对照——存在跨 subgraph 连线时 Mermaid 会忽略内部 `direction`，导致整图横向拉开、被缩到极小。改用多组互不相连的成对箭头（`A1 -.-> B1`、`A2 -.-> B2`），dagre 会自动排成 2 列 N 行。
- 同一层级并列节点 ≤ 2 个；有 3 个时合并为 1 个多行标签节点。
- 单个节点标签每行 ≤ **20 个中文字符**，用 `<br/>` 换行。

## 10. 表达规范

- 中文，第二人称"你"，短句。
- 能用**表格**对比的，不写大段文字。
- 术语首次出现：中文 + 英文原词 + 一句解释。例："副作用（side effect，指渲染之外的操作，比如发请求、改 DOM）"。
- 给结论必须给理由。禁止"这是最佳实践"而不说为什么。
- 不设"术语卡片"章节（明确不需要）。

## 11. 禁止事项

- ❌ "以此类推""同理可得""留给读者练习"——跳步一律不允许，该写就写全。
- ❌ 空洞小结（"本章我们学习了……"）、为凑结构而写的填充章节。
- ❌ 营销式形容词：强大、优雅、丝滑、开箱即用、赋能。
- ❌ 一次性抛出 5 个 API 让读者"感受一下"。
- ❌ 只给正确写法，不给错误写法对照。
- ❌ 编造踩坑案例充数。

## 12. 目录结构与衔接

每套教程独立一个顶层目录，结构固定：

```
<主题>教程/
├── README.md          # 版本基线 + 写给谁 + 路径建议 + 进度表 + 目录
├── docs/
│   ├── ch01-<英文slug>.md
│   └── ch02-<英文slug>.md
└── code/
    ├── ch01/
    └── ch02/
```

`README.md` 必须包含：

1. **版本基线**（引用块，写明各依赖版本 + 语言版本）
2. **这本教程写给谁**
3. **最短路径**（哪几章 + 约几小时 + 做出什么）与**完整路径**（总章数 + 按每天 2 小时估算周期）
4. **进度表**（章节 / 状态：`✅` 已完成 · `📝` 待写 · 当前章加粗）
5. **目录**（分部分，每章带一句话说明 + 链接）

章与章之间：开头 `开始之前` 承接上一章，结尾 `下一章` 指向后一章，形成链条。

## 13. 交付前自查清单

每章交付前逐条确认：

- [ ] 新概念 ≤ 3 个
- [ ] 正文 ≤ 400 行，预计耗时 ≤ 90 分钟
- [ ] 骨架各节齐全、顺序未调整
- [ ] 每个概念走完四步（图解 → 可运行代码 → 拆解结果 → 改坏试试）
- [ ] 每个概念都有反例（什么时候不该用）
- [ ] 代码完整无省略、含文件路径注释、已落盘 `code/chNN/`
- [ ] 前端代码无 `any`；代码已实机验证或已标注未验证
- [ ] 版本号 / API 名 / 命令参数均已核实
- [ ] 类比符合第 7 节分场景规则（前端无 C# 类比）
- [ ] 无公式推导，数学部分用数值示例
- [ ] Mermaid ≤ 2 列、`graph TB`、标签已换行
- [ ] 踩坑警告 ≥ 2 条且真实
- [ ] 动手练习带验收标准
- [ ] 参考答案在文末、分隔线 + 30 空行隔离，正文未提前透露
- [ ] 无第 11 节禁止事项
- [ ] README 进度表已更新
